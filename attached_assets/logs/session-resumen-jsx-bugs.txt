Acá va el resumen de esa sesión, en orden:

**1. Bug de JSX en `PublicWebsite.tsx`**
La app estaba rota por JSX mal armado: una condición de pestaña duplicada/mal ubicada y etiquetas de cierre sueltas (`</section>` sin apertura, mismatch de `<div>`s). Se corrigió la anidación para que el contenido de la pestaña "inicio" (hero, pilares, métricas, bloques de info, cultura, hub de soluciones) quede como un bloque coherente. Se confirmó con code review que quedó sano. No se tocó nada más — ni la conversión a PHP/MySQL ni el tema de WordPress que aparecían mencionados en un archivo subido en esa sesión (eso era historial de otra conversación previa, no un pedido activo).

**Pendientes identificados en ese momento:**
- Errores 401 en consola al cargar `/` (posible llamada a API con auth fallando silenciosamente) — no investigados.
- El tema de WordPress (`clientum-wp-theme/`) parecía a medio camino (un template de servicio individual con formulario de cotización, sin confirmar si se completó).
- La conversión completa a PHP + MySQL — nunca se hizo, la app seguía en React/TS/Express.
- No se revisó visualmente el resto de las pestañas (planes, nosotros, casos, etc.).

**2. Plugin de WordPress — Panel de usuario**
A pedido, se armó `clientum-user-dashboard` (plugin independiente del tema) en `clientum-wp-theme/clientum-user-dashboard/`:
- Sus propias 8 tablas de CRM (se crean al activar el plugin, no depende del tema).
- REST API propia (`clientum/v1`) con CRUD de contactos, empresas, leads, negocios, actividades, productos, facturas, cotizaciones.
- Rutas propias `/app`, `/login`, `/registro`, `/recuperar-contrasena` interceptadas directo por el plugin (funciona con cualquier tema activo).
- Shortcode `[clientum_user_dashboard]` como alternativa.
- Se corrigieron 2 bugs de la revisión de código: rutas rotas en subdirectorio, y API key de WhatsApp guardada en texto plano (ahora cifrada).
- Nota: si el tema Clientum también tiene sus propias plantillas de login/registro/app, puede haber conflicto — usar uno u otro.
- Entregable: `clientum-wp-theme/clientum-user-dashboard.zip`.

**3. XML de importación (WXR)**
Se generó un WXR con 37 páginas (todas las secciones del sitio, ya vinculadas a su plantilla PHP del tema) + 3 menús (`menu-principal`, `menu-footer`, `menu-usuario`) con slugs que `functions.php` auto-asigna. No incluyó Login/Registro/Recuperar/App porque esas rutas ya las sirve el plugin de usuario.

**4. Conversión completa a plugin de WordPress**
Se armó `clientum-ai-prospector` (plugin más grande, reemplazando Express por PHP):
- `class-database.php`: 6 tablas MySQL (deals, activities, contacts, templates, api_keys, leads), API keys cifradas AES-256.
- `class-rest-api.php`: 25+ endpoints REST reemplazando todo Express.
- `class-ai-handler.php`: integración Gemini (brochure, ICP Builder, MEDDIC, outreach, copiloto, traducción).
- `class-scraper.php`: Apify/Google Maps con fallback demo.
- `class-admin.php`: panel de admin con API keys y estado del plugin.
- Frontend React adaptado (`wp-adapter.ts`, `main-wp.ts`) que redirige `/api/*` → `/wp-json/clientum/v1/*` e inyecta nonce.
- Build IIFE autocontenido vía Vite + script `build-for-wp.sh`.
- Requiere permalinks no-"Predeterminado" para que funcione la REST API.

**5. Export de "Soluciones"**
Se agregó un botón "Exportar soluciones · CSV" en la sección "Todas las Soluciones" de la home, generando `clientum-soluciones-12.csv`. Con esto quedaron 4 exports CSV distintos corriendo en paralelo: catálogo (2147 servicios), planes/precios, cursos del campus (67), y soluciones (12).

**6. Último pedido (quedó a mitad de camino)**
"Fusiona todo en uno solo para importar items en WooCommerce" — se estaba reemplazando las 4 funciones de export individuales por una sola función unificada que genera un formato importable a WooCommerce, y unificando los 4 botones en uno solo. Quedó ahí cortado, sin confirmación de que se haya terminado.

¿Querés que sigamos esa última tarea acá, o tenés otra cosa en mente?